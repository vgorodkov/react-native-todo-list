import { StyleSheet } from 'react-native';

import { spacing } from '@/constants/spacing';

export const styles = StyleSheet.create({
  listItem: { flex: 1, paddingHorizontal: spacing.l, paddingVertical: spacing.s },
});
