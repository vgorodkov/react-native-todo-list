import { StyleSheet } from 'react-native';

import { spacing } from '@/constants/spacing';

export const styles = StyleSheet.create({
  listItem: { flex: 1, width: 320, paddingHorizontal: 24, paddingVertical: spacing.s },
});
