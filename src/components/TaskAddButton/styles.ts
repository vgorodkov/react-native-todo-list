import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  taskAddBtnIconContainer: {
    backgroundColor: '#646FD4',
    padding: 12,
    borderRadius: 100,
  },
  taskAddBtnIcon: { width: 32, height: 32 },
});
