import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  checkbox: {
    width: 24,
    height: 24,
    borderWidth: 1,
    borderRadius: 100,
    justifyContent: 'center',
    alignItems: 'center',
  },
  checkboxTickIcon: { width: 22, height: 22 },
});
